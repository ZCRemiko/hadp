# ================================================================
# Hadoop 集群环境变量配置文件
# 被 docker-compose.yml 中所有 Hadoop 容器共享
#
# 变量命名规则: <SECTION>_CONF_<property_name>
#   CORE_CONF_*   -> 写入 core-site.xml
#   HDFS_CONF_*   -> 写入 hdfs-site.xml
#   YARN_CONF_*   -> 写入 yarn-site.xml
#   MAPRED_CONF_* -> 写入 mapred-site.xml
#
# 属性名中的双下划线 __ 会被替换为 .（点号）
# 属性名中的三个下划线 ___ 会被替换为 -（横线）
# 属性名中的四个下划线 ____ 会被替换为 @（at符号）
# ================================================================

# ---------- core-site.xml ----------
# HDFS 默认文件系统地址：告诉所有客户端 NameNode 的位置
CORE_CONF_fs_defaultFS=hdfs://namenode:9000

# ---------- hdfs-site.xml ----------
# 开启 WebHDFS（REST API 方式访问 HDFS）
HDFS_CONF_dfs_webhdfs_enabled=true
# 关闭权限检查（开发环境，方便测试）
HDFS_CONF_dfs_permissions_enabled=false
# 关闭 DataNode 注册时的 IP 主机名校验（Docker 网络环境必需）
HDFS_CONF_dfs_namenode_datanode_registration_ip___hostname___check=false

# ---------- yarn-site.xml ----------
# 开启日志聚合（MapReduce任务日志集中存储）
YARN_CONF_yarn_log___aggregation___enable=true
# ResourceManager 地址
YARN_CONF_yarn_resourcemanager_hostname=resourcemanager
YARN_CONF_yarn_resourcemanager_address=resourcemanager:8032
YARN_CONF_yarn_resourcemanager_scheduler_address=resourcemanager:8030
YARN_CONF_yarn_resourcemanager_resource__tracker_address=resourcemanager:8031
# 使用 CapacityScheduler（容量调度器）
YARN_CONF_yarn_resourcemanager_scheduler_class=org.apache.hadoop.yarn.server.resourcemanager.scheduler.capacity.CapacityScheduler

# ---------- mapred-site.xml ----------
# 指定 MapReduce 使用 YARN 作为资源管理器
MAPRED_CONF_mapreduce_framework_name=yarn
